import React from "react";
import { Text, View } from "react-native";

const ProfileComponent = () => {
  return (
    <View>
      {/* Header Bar */}
      <View>
        <Text className="text-lg text-white font-bold">Profile</Text>
      </View>
    </View>
  );
};

export default ProfileComponent;
